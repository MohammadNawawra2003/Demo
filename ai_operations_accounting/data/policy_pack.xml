<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <!--
        Accounting Intelligence.

        OWNER DECISION 2026-09-11 (decision-log DL-010), superseding the
        read-only line of 2026-09-07. The Accountant now DRAFTS: a vendor bill
        and a journal entry, each left in draft for a person to confirm. That is
        Level 2 (Prepare), the ceiling every operational agent works at.

        What did not move:

          * nothing is posted, paid, reconciled, taxed or banked by an agent.
            account.move.line, account.payment, account.journal, account.tax
            and res.partner.bank hold no permission here, and the ledger
            machinery test still asserts that for every profile. A draft's
            lines are written through account.move, as the person who asked.
          * the four operational agents still cannot read accounting at all
            (Document B section 11 rows 1, 2 and 4; section 13 X-04).
          * one action, CREATE_DRAFT on account.move, and it carries an amount
            ceiling: 100,000 in company currency. The operator changes it on
            the agent form, Action Permissions tab, Max Amount. A bill is
            measured on its total with tax, an entry on its debits.

        This file is noupdate. On an existing database the new records below are
        created and the changed ones are moved by migrations/19.0.1.3.0.
    -->
    <record id="profile_accounting" model="ai.operations.agent.profile">
        <field name="name">Accounting Intelligence</field>
        <field name="code">accounting</field>
        <field name="active" eval="False"/>
        <field name="max_autonomy_level">2</field>
        <field name="max_write_ops">2</field>
        <field name="policy_version">1.2.0</field>
        <field name="description">Financial reporting and draft bookkeeping. To report, call your tools: receivable ageing, payable ageing, unpaid customer invoices, and invoiced revenue by period. To record, prepare drafts: a vendor bill, for example from a bill image the user attaches, or a journal entry. Find the vendor with find_partners and the account codes with find_accounts first, then call the draft tool once. You cannot post: every draft waits for an accountant to review and confirm it, so say so, and report the totals Odoo computed next to the document's. You cannot register a payment, reconcile, change a tax or touch bank data.</field>
    </record>

    <record id="perm_account_move_a" model="ai.operations.model.permission">
        <field name="profile_id" ref="profile_accounting"/>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_create" eval="True"/>
    </record>

    <record id="perm_res_partner_a" model="ai.operations.model.permission">
        <field name="profile_id" ref="profile_accounting"/>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="perm_read" eval="True"/>
    </record>

    <record id="perm_res_currency_a" model="ai.operations.model.permission">
        <field name="profile_id" ref="profile_accounting"/>
        <field name="model_id" ref="base.model_res_currency"/>
        <field name="perm_read" eval="True"/>
    </record>

    <!-- Read only: the drafts name accounts by code. Never create or write. -->
    <record id="perm_account_account_a" model="ai.operations.model.permission">
        <field name="profile_id" ref="profile_accounting"/>
        <field name="model_id" ref="account.model_account_account"/>
        <field name="perm_read" eval="True"/>
    </record>

    <record id="action_create_draft_move" model="ai.operations.action.permission">
        <field name="profile_id" ref="profile_accounting"/>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="action_code">CREATE_DRAFT</field>
        <field name="allowed" eval="True"/>
        <field name="autonomy_required">2</field>
        <field name="max_amount">100000</field>
        <field name="risk_level">HIGH</field>
    </record>

</odoo>
