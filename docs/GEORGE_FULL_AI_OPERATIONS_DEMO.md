# دليل العرض الكامل — Naqaa Water · AI Operations

**AI Operations — Customer Demo Runbook**

> **الحالة: ✅ جاهز للعرض.** البناء `6a5e783` على staging — جولة ملاحظات جورج على مسار
> التسليم بين الأقسام. الجولتان **#16 و#17** اكتملتا
> **١٠ من ١٠ خطوات، صفر رفض غير متوقع**، وسلسلة الاستلام البشرية أُغلقت بالكامل
> (فجوة صفر). كل رقم في هذا المستند **مقروء من قاعدة بيانات staging الحيّة يوم 2026-09-09**،
> لا منقول من دليل سابق.
>
> **هذا ليس دليل مستخدم.** هذا نصّ عرض: من تسجّل دخوله، وماذا تكتب حرفياً، وماذا يجب أن يظهر،
> ومن يأتي بعده. لا تخترع أسئلة أمام العميل.

---

## ١. الفكرة التي تبيعها، في سطرين

هذه **ليست «ذكاء اصطناعي داخل أودو»**. هذه **نواة صلاحيات** يعمل الذكاء الاصطناعي داخلها.
الوكيل الذي يُطلب منه ما هو خارج نطاقه يفشل عند فحص صلاحيات الـ ORM — **ويفشل بنفس الطريقة
تماماً لو أعاد أحدهم كتابة تعليماته**. أقصى استقلالية هي **المستوى ٢ «تحضير»**: الوكيل يُحضّر
مسودة، والإنسان يضغط «تأكيد».

```
EFFECTIVE = USER ∩ AGENT ∩ TOOL ∩ ACTION ∩ COMPANY
```

الجملة التي تقولها للعميل في النهاية: **«الوكيل لا يستطيع أن يفعل ما لا يستطيع الموظف فعله.»**

---

## ٢. قبل العرض — قائمة التحقق

| # | البند | كيف تتحقق |
|---|---|---|
| 1 | البناء على staging = آخر commit | صفحة Builds في Odoo.sh |
| 2 | الخادم | `ksa-ai-stage-37686456.dev.odoo.com` · قاعدة البيانات `ksa-ai-stage-37686456` |
| 3 | كلمات مرور مستخدمي العرض مضبوطة | جرّب دخول `khalid.m` قبل العرض بساعة |
| 4 | مفتاح المزوّد يصل إلى الـ worker | اسأل أي وكيل سؤالاً بسيطاً؛ ردّ فارغ = المفتاح |
| 5 | **العملة = ريال سعودي** | افتح أي أمر شراء: يجب أن يظهر **SR** لا `$` |
| 6 | الشركة النشطة = **Naqaa Water Manufacturing Co.** | مبدّل الشركة أعلى اليمين |
| 7 | تسجيل الدخول **كموظف**، لا كـ `admin` | انظر التحذير أدناه |
| 8 | نسخة **Enterprise** | وحدات الجودة غير متاحة في Community |
| 9 | **إعادة التهيئة نُفِّذت** | §٩ — قبل العرض بساعة، لا قبله بدقيقة |
| 10 | **وحدة `ai_operations_chat_widget` مثبَّتة** | بدونها **لا توجد فقاعة محادثة أصلاً** — انظر التحذير |

> ⚠️⚠️ **`ai_operations_demo_data` لا يعتمد على `ai_operations_chat_widget`.** تثبيت وحدة
> بيانات العرض وحدها (`-i ai_operations_demo_data`) يبني السيناريو كاملاً **بلا واجهة محادثة**،
> فيقف مقدّم العرض أمام نظام صحيح لا يستطيع الحديث إليه. staging مثبَّتة عليه الوحدات الاثنتا
> عشرة كلها، لكن أي قاعدة بيانات جديدة تُبنى للعرض يجب أن تُثبَّت الوحدة فيها صراحةً.
> (اكتُشف أثناء اختبار تثبيت نظيف يوم 2026-09-09.)

> ⚠️⚠️⚠️ **لا تعرض بحساب `admin`.** المستخدم `admin` يحمل `base.group_system` وهو يجتاز كل فحص
> وصول، فينهار حدّ المستخدم من المعادلة ويتحول `USER ∩ AGENT ∩ TOOL ∩ ACTION ∩ COMPANY` إلى
> أربعة حدود بدل خمسة. **عندما تكون النقطة هي الحدّ الأمني، اعرض بحساب موظف.**

> ⚠️⚠️⚠️ **البروفة تستهلك ميزانية العرض نفسها.** لكل وكيل سقف **200,000 وحدة يومياً**.
> الجولة الكاملة تكلّف نحو **87,600 وحدة** (التصنيع أثقلها بنحو 60,000) — أي **ثلاث جولات
> في اليوم، لا أربع**. ستّ جولات في يوم واحد أوصلت وكيل التصنيع إلى 204,620 فتوقّف عن الرد.
> **إن ردّ وكيل بنص الرفض المجمَّد على سؤال عادي كان يعمل صباحاً، افحص العدّاد أولاً.**
> إعادة التهيئة في §٩ تصفّر العدّاد اليومي. **السقف نفسه لا يُمسّ.**

> ⚠️ **مبدّل الشركة.** الشاشات تظهر فارغة إذا كانت الشركة النشطة «My Company». البيانات
> موجودة وغير مرئية. هذا سبب لقطة الشاشة الفارغة في مراجعة سابقة.

> ⚠️ **زر المحادثة** هو فقاعة الكلام في شريط النظام العلوي، يمين أيقونة الرسائل.

> ⚠️ **معرّفات السجلات تتغيّر مع كل إعادة بناء.** لا تحفظ رقماً؛ §١٠ يشرح كيف تستعيده.

---

## ٣. من يدخل، وأي وكيل يفتح

القيم مقروءة من staging.

| الوكيل | الموظف | الاسم | الاستقلالية | كتابة | أدوات |
|---|---|---|---|---|---|
| Manufacturing Intelligence | `khalid.m` | Khalid Al-Shehri | 2 | 2 | 8 |
| Procurement Intelligence | `noura.p` | Noura Al-Harbi | 2 | 2 | 12 |
| Procurement (قراءة فقط) | `fahad.p` | Fahad Al-Otaibi | 2 | 2 | 12 |
| Inventory Intelligence | `mansour.i` | Mansour Al-Ghamdi | 2 | 2 | 10 |
| Quality Intelligence | `rania.q` | Rania Al-Malki | 2 | 2 | 8 |
| General Manager Intelligence | `faisal.gm` | Faisal Al-Rasheed | **0** | **0** | 6 |
| Accounting Intelligence | `omar.f` | Omar Al-Dosari | **0** | **0** | 4 |

جميع الوكلاء: المزوّد `anthropic`، النموذج `claude-sonnet-5`، وحدّ **٨ استدعاءات أدوات**
في الدور الواحد.

**`allow_autonomous` (مقروء من staging 2026-09-09):** مفعَّل على **المشتريات والتصنيع
والمخزون** — الثلاثة التي تستقبل عملاً من قسم آخر. **متوقّف** على **الجودة والمدير العام
والمحاسبة**.

> ⚠️ **هذا لا يعني أن وكيلاً يعمل من تلقاء نفسه.** الراية تسمح بشيء واحد: أن يفتح الوكيل
> **عملاً أرسله إليه قسم آخر**، مرة واحدة، فور وصوله. لا مهمة مجدولة، ولا استطلاع دوري،
> ولا سلسلة تمرير — القفزة الثانية يرفضها `HANDOFF_CASCADE_BLOCKED`. **المهمة اليومية
> المجدولة ما زالت متوقفة.**

> ✅ **كل موظف يرى وكيله فقط.** عند فتح فقاعة المحادثة، تعرض القائمة **الوكيل المسنَد إلى هذا
> الموظف لا غير**: نورة ترى «المشتريات» فقط، وخالد يرى «التصنيع» فقط. الإسناد يُضبط من
> **AI Operations ← الإعدادات ← ملفات الوكلاء ← المستخدمون المسموح لهم**.
>
> **ما تقوله للعميل إن سأل:** «الإسناد طبقة إضافية فوق الأمان، لا بديلاً عنه. حتى لو فُتح وكيل
> بطريقة أخرى، تبقى صلاحيات الموظف نفسها هي الحاكمة.»
>
> ⚠️ **فهد استثناء مقصود:** هو مُسنَد إلى وكيل المشتريات **عمداً**، لأن مشهد الرفض في §٨ يحتاجه
> أن يصل إلى الحارس ثم يُرفض على حدّ **المستخدم**. لو أُخفي الوكيل عنه لتحوّل أقوى مشهد في العرض
> إلى قائمة فارغة.

> ⚠️ محلل الجودة `rania.q` **لا يستطيع** رفع `QUALITY_HOLD_PRODUCTION` لأنه لا يملك قراءة
> `mrp.production`. هذا `USER ∩ AGENT` يعمل كما ينبغي. استخدم `huda.q` لتلك الخطوة.

> ⚠️ **حسابات `AI /` الستة ليست موظفين.** هي هويات تنفيذ للوكلاء، ولا يمكن الدخول بها إطلاقاً
> (لا كلمة مرور ولا مفتاح API، وفحص الاعتماد يرفضها مباشرة). تظهر الآن بعلامة **AI Service User**
> على بطاقة المستخدم. **لا تحذفها** — أودو يمنع الحذف وتتعطّل ترقية وحدة العرض.

> ⚠️ **Discuss يعرض المحادثة بأعضائها لا باسمها المخزّن.** لا تبحث عن القنوات بالاسم.

---

## ٤. البيانات — لماذا هذه بالذات

طلب بيع واحد يولّد أمر تصنيع ناقصاً **مكوّناً واحداً فقط**. مكوّن واحد يروي قصة؛ خمسة نواقص
لا تُظهر أي حكم.

| العنصر | القيمة (staging، مؤكَّدة 2026-09-09) |
|---|---|
| طلب البيع | **`S00662`** — ٥٠٠ كرتونة `FG-600-E2E` إلى **Naqaa Distribution Co.**، مؤكَّد |
| أمر التصنيع | **`RM/MO/00002`** — أُنشئ تلقائياً عبر مسار MTO، الحالة `confirmed` |
| أمر التسليم | **`RM/OUT/00002`** — الحالة `waiting`، ينتظر الإنتاج |
| المكوّن الناقص | **`PK-BTL-600`** — مطلوب **12,000**، محجوز **8,000**، النقص **4,000** |
| بقية المكوّنات | خمسة، جميعها `assigned` (فجوة صفر) |
| المورّدان | Jeddah Plastic Industries `SUP-JPI` **0.078 SAR / 18 يوماً** · Riyadh PET Co. `SUP-RPC` **0.0827 SAR / 21 يوماً** — الحد الأدنى للطلب **صفر** لكليهما |
| طلب البيع المقابل | **`S00663`** — ١٠٠ كرتونة `FG-600` (مخزون وافر، لا عمل) |

`PK-BTL-600` مُختار عمداً: هو المكوّن الوحيد بمورّدَين حقيقيين، فيكون أمام أداة
`compare_suppliers` **قرار فعلي** لا سطر واحد.

> ⚠️ **`PK-BTL-600` يظهر بـ 230,400 وحدة في المخزون والأمر ما زال ناقصاً.** مستودع مختلف:
> الأمر يسحب من **مخزن المواد الخام `RM/Stock`** الذي يحوي **8,000** (كلها محجوزة)؛
> الـ 230,400 في `WIP/Stock`. **هذا سؤال سيطرحه العميل — جهّز الإجابة.**

---

## ٥. العرض في ١٠ دقائق

العمود الفقري للذكاء الاصطناعي فقط: **الخطوات ٢ → ٣ → ٤ → ٥ → ٦ → مشهد الرفض (§٨).**

نقص يُكتشف · تسليم بين قسمين · **إشعار يصل إلى الموظّفة باسمها** · وكيل المشتريات يقارن
الموردين ويحضّر مسودة **من تلقاء الطلب الوارد** · اعتماد إنسان · ثم رفض يُظهر الحدّ.

لا تدخل سلسلة الاستلام (٧أ–٧ج) ولا الإنتاج في هذه النسخة.

---

## ٦. العرض الكامل (٢٠–٣٠ دقيقة)

### الخطوة ١ — الطلب الذي بدأ كل شيء (بدون ذكاء اصطناعي)

- **المستخدم:** `faisal.gm` · **الشاشة:** المبيعات ← الطلبات ← **`S00662`**
- **الوكيل:** لا شيء. هذه خطوة بشرية تُمهّد القصة.
- **ما تُظهره:** ٥٠٠ كرتونة `FG-600-E2E` إلى **Naqaa Distribution Co.**، الحالة «أمر بيع».
  ثم اضغط الزر الذكي **التصنيع** لتُظهر **`RM/MO/00002`** الذي أنشأه أودو تلقائياً.
- **ما تقوله للعميل:** «الطلب من التوزيع ولّد أمر تصنيع بنفسه. لم يتدخل أحد بعد — ولا الذكاء
  الاصطناعي. هذه أودو العادية. الآن نرى ماذا يضيف الوكيل.»
- **⛔ لا تضغط:** لا تفتح أمر التصنيع وتضغط «تحقق من التوفر» — الوكيل هو من سيكتشف النقص.

### الخطوة ٢ — التصنيع يكتشف النقص

- **المستخدم:** `khalid.m` · **الوكيل:** Manufacturing Intelligence
- **الشاشة:** أي شاشة ← فقاعة المحادثة في شريط النظام

```
ما هي أوامر التصنيع المفتوحة؟ وهل أمر التصنيع الخاص بمنتج FG-600-E2E جاهز للإنتاج؟
```

> *English:* `Which manufacturing orders are open? And is the order for FG-600-E2E ready to produce?`

- **الأدوات المتوقعة:** `manufacturing.get_open_mos` ثم `manufacturing.check_readiness`
- **الإجابة المتوقعة:** الأمر **غير جاهز**؛ النقص في **`PK-BTL-600`**: مطلوب **12,000**،
  متوفّر **8,000**، العجز **4,000**. بقية المكوّنات كافية.
- **السجل الناتج:** لا شيء. قراءة فقط.
- **سجل التدقيق:** `OPEN → DECISION(ALLOWED) → RESULT`، بلا صف `ERROR`.
- **ما تقوله:** «لم نكتب شيئاً. الوكيل قرأ الحالة الحقيقية من أودو، لا من ذاكرته.»
- **القسم التالي:** المشتريات — لكن التصنيع هو من يُسلّم العمل.

### الخطوة ٣ — التصنيع يسلّم العمل، والمشتريات تبدأ وحدها

- **المستخدم:** `khalid.m` · **الوكيل:** Manufacturing Intelligence
- **⚠️⚠️⚠️ نفّذ الخطوة ٢ أولاً في القناة نفسها. لا تشغّل هذه الخطوة وحدها.**

```
ارفع طلب تسليم إلى قسم المشتريات بخصوص النقص في عبوات ٦٠٠ مل
```

> *English:* `Raise a handoff to the Procurement department about the 600 ml bottle shortage.`

> ⚠️⚠️⚠️ **هذه الجملة وحدها تُنتج أمر شراء خاطئاً بالكامل.** الجملة لا تسمّي أمر تصنيع،
> والوكيل يعرف أمر `RM/MO/00002` **من سياق الخطوة ٢ فقط**. وإعادة التهيئة (§٩) **تمسح سجل
> المحادثة**، فإن شغّلت هذه الخطوة بعد إعادة تهيئة دون الخطوة ٢ فالوكيل يستعرض قائمة الأوامر
> المفتوحة ويختار منها.
>
> **والاحتمال ليس ضئيلاً — هو الأرجح.** على staging **مئة أمر تصنيع مفتوح**، **أربعة عشر**
> منها ناقصة من `PK-BTL-600` نفسه بفجوات تصل إلى **312,672** وحدة، و**أمر السيناريو ترتيبه
> العشرون** في القائمة التي تُسلَّم للوكيل. أن يختار غيره هو النتيجة المتوقَّعة، لا سوء حظ.
>
> **مقيس يوم 2026-09-09:** الخطوة ٣ وحدها أنتجت طلب تسليم على `WIP/MO/00223` بكمية
> **138,240** وحدة ومسودة أمر شراء بـ **10,782.72 ريال**. الخطوة ٢ ثم الخطوة ٣ في القناة
> نفسها أنتجت `RM/MO/00002` وكمية **4,000** و**312.00 ريال**. **الترتيب ليس تجميلاً.**
>
> ⚠️ **والمسودة الخاطئة تصل بلا أي علامة تحذير.** راية `ai_approval_required` تُرفع من
> **فرق** بين الكمية المطلوبة والكمية المقيسة؛ وفي المسار التلقائي يأخذ وكيل المشتريات
> الطرفين من حمولة طلب التسليم نفسها، فالفرق صفر مهما بلغت القيمة. **مسودة بـ 10,782 ريال
> تبدو روتينية تماماً كمسودة بـ 312.** الضوابط التي تهمّ صمدت — بقيت مسودة، والاعتماد بيد
> إنسان، وكل شيء في سجل التدقيق — لكن **لا تعتمد على شكل الشاشة لاكتشاف الخطأ.**

- **الأدوات المتوقعة:** `manufacturing.find_product` ← `manufacturing.check_readiness`
  ← `manufacturing.raise_handoff` بنوع **`MATERIAL_SHORTAGE`**
- **حمولة طلب التسليم المتوقعة:** `origin_ref = RM/MO/00002` · مطلوب **12,000** ·
  متوفّر **8,000** · **النقص 4,000**
- **✅ لا تحتاج إلى كتابة رقم السجل.** لم يعد مقدّم العرض ينسخ رقماً — الإشعار يصل بنفسه.

> 📌 **الأداة لا تصدّق الأرقام التي يقولها النموذج.** لو ادّعى النموذج كمية مختلفة، تستبدلها
> الأداة بالأرقام المقروءة من أمر التصنيع نفسه. تم التحقق: النموذج مرّر 230,400 فاستبدلتها
> الأداة بالقيمة الحقيقية للأمر الذي اختاره.

- **ما تقوله:** «الوكيل لا يشتري. ينقل العمل إلى القسم صاحب الصلاحية — والقسم الآخر يبدأ فوراً.»

### الخطوة ٤ — الموظّفة تُبلَّغ (هذه هي الإجابة على ملاحظة جورج)

- **المستخدم:** **بدّل الآن إلى `noura.p`** · **الشاشة:** أي شاشة

**لا تكتب شيئاً في هذه الخطوة.** فقط أرِ الشاشة:

1. **ساعة الأنشطة** في الشريط العلوي تحمل عنصراً جديداً.
2. **فقاعة المحادثة** تحمل عدّاداً، وتفتح نفسها مرة واحدة على العمل الوارد.
3. افتح سجل طلب التسليم: **المحادثة (chatter)** أسفله تحمل الطلب، **والنشاط مُسنَد باسم
   نورة الحربي** بنوع **AI Review Required** ونصّ
   «Incoming request from Manufacturing Intelligence: AIH/2026/000NN».

- **ما تقوله:** «هذا بالضبط ما كان ناقصاً: طلب التسليم لم يعد صفّاً في قائمة ينتظر أن يلاحظه
  أحد. الموظّفة المسؤولة أُبلغت باسمها، والعمل ظهر في نفس المكان الذي تتابع فيه بقية مهامها.»

### الخطوة ٥ — ما فعله وكيل المشتريات وحده

**الوكيل المستقبِل عمل قبل أن تفتح أنت شيئاً.** أرِ النتيجة، لا تكتب طلباً جديداً:

- **الأدوات التي نُفِّذت تلقائياً، بهذا الترتيب:**
  `accept_handoff` ← `find_production` ← `compare_suppliers` ← `get_shortage_context`
  ← `prepare_draft_rfq` ← `create_review_activity` ← `complete_handoff`
- **السجل الناتج:** مسودة أمر شراء — `PK-BTL-600` × **4,000** بسعر **0.078** من
  **Jeddah Plastic Industries**، الإجمالي **312.00 ريال سعودي**، الحالة **طلب عرض سعر**.
- **ونشاط ثانٍ على أمر الشراء نفسه** مُسنَد إلى نورة: «… needs approval».
- **حالة طلب التسليم:** `COMPLETED`.
- **طلب تسليم واحد فقط.** لا سلسلة ولا تمرير ذهاباً وإياباً — `HANDOFF_CASCADE_BLOCKED`
  يرفض القفزة الثانية.

> 📌 **هوية التنفيذ:** هذا الدور جرى بمُشغّل **HANDOFF** ووضع **AUTONOMOUS** وهوية
> **`AI / Procurement`** — لا بحساب نورة. افتح سجل التدقيق وأرِ العمود: العمل الذي بدأ بنفسه
> يحمل هوية الخدمة، لا هوية موظّف لم يطلبه.

- **ما تقوله:** «القسم الثاني لم ينتظر أحداً. قارن الموردين، حسب النقص الفعلي لهذا الأمر
  بالذات، وحضّر مسودة — ثم **توقّف** وطلب موافقة إنسان.»

> ⚠️ **`Allow Autonomous` صار مفعّلاً على الوكلاء التي تستقبل عملاً** (المشتريات والتصنيع
> والمخزون). إن سأل العميل: **هذا لا يعني أن الوكيل يعمل من تلقاء نفسه.** يعني شيئاً واحداً:
> أن يفتح عملاً أرسله قسم آخر، **مرة واحدة**. المهمة المجدولة اليومية ما زالت **متوقفة**.

> 📌 **مقارنة الموردين لم تعد خطوة يكتبها مقدّم العرض.** كانت الخطوة ٦ في النسخة السابقة؛
> صارت جزءاً من الدور التلقائي أعلاه. الأداة ما زالت موجودة — يمكنك أن تسأل وكيل المشتريات
> «قارن بين موردي PK-BTL-600» وسيجيب — لكن مسار العرض لم يعد يحتاج ذلك.

### الخطوة ٦ — الإنسان يعتمد

- **المستخدم:** `noura.p` · **الشاشة:** افتح أمر الشراء **من النشاط نفسه** (ساعة الأنشطة ←
  «Draft PO … needs approval») — أو المشتريات ← طلبات عروض الأسعار ← المسودة من الخطوة ٥
- **الإجراء البشري:** افتح المسودة ← تحقق أنها **312.00 SR** ← اضغط **تأكيد الطلب**
- **ما تقوله:** «هذه هي الحدود. المستوى ٢ يعني **تحضير**، لا تنفيذ. لا يوجد إعداد يجعل الوكيل
  يضغط هذا الزر — الصلاحية غير ممنوحة أصلاً.»
- **النتيجة:** الحالة `purchase`، ويُنشأ **إذن استلام** تلقائياً. **افتح الزر الذكي «الاستلام»
  واكتب رقمه** (نموذج `RM/IN/000NN`).

### الخطوات ٧أ–٧ج — إغلاق سلسلة الاستلام (ثلاث خطوات بشرية)

> ⚠️⚠️⚠️ **بدون هذه الثلاث لا تُغلق السلسلة ويقف مقدّم العرض أمام العميل.** كلها **يقوم بها
> إنسان**، لا الوكيل. المستخدم: `mansour.i` أو `salem.i`.

**٧أ — رقم الدفعة.** `PK-BTL-600` مُتتبَّع بالدفعات. تأكيد الاستلام يرفض بالرسالة
*"You need to supply a Lot/Serial number for products [PK-BTL-600]"*. اكتب أي رقم دفعة،
مثلاً `LOT-DEMO-<التاريخ>`.

**٧ب — فحوصات الجودة.** الاستلام مقيَّد بمراقبة الجودة: `button_validate` يفتح
`quality.check.wizard`. كان على إذن الاستلام في الجولات السابقة **عشرة فحوصات** نجاح/فشل،
**يجب تمريرها جميعاً** قبل أن يُغلق الاستلام.

**٧ج — التحويل إلى المخزن.** مستودع المواد الخام **من خطوتين**. البضاعة تصل إلى **`RM/Input`**
لا `RM/Stock`، فيبقى أمر التصنيع عاجزاً عن الحجز. أكّد إذناً ثانياً من نوع Storage
(نموذج `RM/STOR/000NN`).

- **ما تقوله:** «كل واحدة من هذه الثلاث قرار إنسان: من وقّع على الدفعة، من أجاز الجودة، من
  أدخل البضاعة للمخزن. الوكيل حضّر الطلب فقط.»

### الخطوة ٨ — التصنيع يتأكد أن النقص أُغلق

- **المستخدم:** `khalid.m` · **الوكيل:** Manufacturing Intelligence

```
هل أمر التصنيع RM/MO/00002 جاهز للإنتاج الآن؟
```

> *English:* `Is manufacturing order RM/MO/00002 ready to produce now?`

- **الإجابة المتوقعة:** **جاهز** — `PK-BTL-600` مطلوب 12,000 / محجوز **12,000** / الفجوة **صفر**،
  وحالة الحجز `assigned`، والمكوّنات الستة كلها كافية.
- **ما تقوله:** «نفس السؤال، نفس الوكيل، نفس الأداة — إجابة مختلفة، لأن الواقع تغيّر.
  الوكيل لا يتذكّر، بل يقرأ.»

### الخطوة ٩ — الإنتاج (بشري)

- **المستخدم:** `khalid.m` أو `yousef.m` · **الشاشة:** التصنيع ← `RM/MO/00002`
- **الإجراء البشري:** **إنتاج الكل** ← **وضع علامة «تم»**
- **ما تقوله:** «الوكيل لم ينتج شيئاً ولن ينتج. لا يملك صلاحية كتابة واحدة على أمر التصنيع.»
- **⛔ لا تضغط هذا في نسخة الـ ١٠ دقائق** — يستهلك خط الأساس ويحتاج إعادة تهيئة.

### الخطوة ١٠ — الجودة

- **المستخدم:** `rania.q` · **الوكيل:** Quality Intelligence

```
هل توجد نتائج فحص خارج المواصفات؟ وما هي الدفعات المتأثرة؟
```

> *English:* `Are there any out-of-spec quality results? Which lots are affected?`

- **الأداة المتوقعة:** `quality.get_out_of_spec`
- **الإجابة المتوقعة:** **لا نتائج خارج المواصفات** ولا دفعات متأثرة.
- **ما تقوله:** «صلاحية الكتابة الوحيدة للجودة هي تنبيه جودة، ومقيّدة بالمرحلة `New` فقط.»

### الخطوة ١١ — المخزون الجاهز والتسليم (بشري)

- **المستخدم:** `mansour.i` · **الشاشة:** المخزون ← أوامر التسليم ← **`RM/OUT/00002`**
- **ما تُظهره:** الحالة انتقلت من `waiting` إلى **جاهز** بعد اكتمال الإنتاج.
- **الإجراء البشري (اختياري):** تحقّق ← تسليم ٥٠٠ كرتونة إلى **Naqaa Distribution Co.**
- **ما تقوله:** «الطلب الذي بدأ العرض وصل إلى التوزيع. الدورة اكتملت.»

### الخطوة ١٢ — المدير العام (قراءة فقط)

- **المستخدم:** `faisal.gm` · **الوكيل:** General Manager Intelligence

```
أعطني ملخصاً تشغيلياً لهذا اليوم: الإنتاج المتوقف والمشتريات المتأخرة وأي نقص في المخزون
```

> *English:* `Give me an operational summary for today: blocked production, late purchasing, and any stock shortages.`

- **الأدوات المتوقعة:** `get_blocked_production` · `get_late_procurement` · `get_stock_exceptions`
- **ما تقوله:** «هذا الوكيل لا يملك ولا صلاحية كتابة واحدة. **صفر.** وهذا مضبوط بالإعداد —
  استقلالية صفر، ميزانية كتابة صفر، صفر صلاحيات إجراء — لا بتعليمات نصية.»

**إن سألته عن المال** (`gm.get_financial_headlines`) فالقيم المؤكَّدة على staging:

| الحقل | القيمة |
|---|---|
| `receivable_outstanding` | **367,145.85** |
| `receivable_overdue` | **247,735.07** |
| **`receivable_overdue_count`** | **17** |
| `payable_outstanding` / `payable_overdue` | **0.00** |
| العملة | **SAR** |

> ✅ **هنا مصدر الرقم «١٧».** هو عدد الفواتير **المتأخرة**، لا عدد الفواتير القائمة (٢٥).
> المدير العام والمحاسبة يعطيان **نفس الإجماليات**، وهذا هو المقصود: مصدر واحد للحقيقة.

> ⚠️ **الذمم الدائنة صفر وهذا صحيح** — أوامر الشراء لم تُفوتر بعد. حالة حقيقية، لا ثغرة.

### الخطوة ١٣ — المحاسبة (قراءة فقط)

- **المستخدم:** `omar.f` · **الوكيل:** Accounting Intelligence

```
استخدم أداة تقادم الذمم المدينة وأعطني النتيجة
```

> *English:* `Use the receivables ageing tool and give me the result.`

- **الأداة المتوقعة:** `accounting.get_receivable_ageing`
- **الإجابة المتوقعة (staging، 2026-09-09):**

**هذا ما تُعيده الأداة حرفياً** (مقروء من staging يوم 2026-09-09، لا محسوباً يدوياً):

| الشريحة | عدد الفواتير | المبلغ (SAR) |
|---|---|---|
| `0-30 days` | 8 | **129,019.25** |
| `31-60 days` | 8 | **119,825.44** |
| `61-90 days` | 2 | **21,320.66** |
| `91+ days` | 0 | **0.00** |
| **`total`** | — | **367,145.85** |
| **`total_overdue`** | — | **247,735.07** |
| العملة | — | **SAR** |

> ⚠️⚠️ **الشرائح لا تُجمَع إلى الإجمالي، وهذا صحيح.** مجموع الشرائح 270,165.35 بينما الإجمالي
> 367,145.85: الفرق فواتير **لم تُستحق بعد** فلا تدخل في جدول التقادم. و`total_overdue`
> (247,735.07) يستخدم تعريفاً أضيق من شريحة `0-30` — وهي تضم فواتير قريبة الاستحقاق لم تتأخر.
>
> **لا تجمع الأعمدة أمام العميل.** إن سُئلت: «جدول التقادم يعرض ما استُحق؛ الإجمالي يعرض كل
> القائم؛ والمتأخر هو ما تجاوز تاريخه.»

> ⚠️⚠️ **قل هذا بصدق:** هذه الذمم من **تاريخ المبيعات البيني المُولَّد** بين شركة التصنيع
> وشركة التوزيع — **ليست من طلب البيع الذي عرضناه للتو**. لا تقل «انظروا، طلبنا أنتج هذا الرقم».

> ⚠️ **الذمم الدائنة تساوي 0.00 وهذا صحيح.** أوامر الشراء موجودة لكنها لم تُفوتر بعد،
> فلا شيء مستحقاً لمورّد. **حالة حقيقية، لا ثغرة.**

---

## ٧. مشهد الكفاية — الوكيل الذي لا يفعل شيئاً

- **المستخدم:** `khalid.m` · **الوكيل:** Manufacturing Intelligence

```
لدينا طلب بيع بكمية 100 كرتونة من FG-600. هل نحتاج إلى أمر تصنيع لتغطيته؟
```

> *English:* `We have a sales order for 100 cartons of FG-600. Do we need a manufacturing order to cover it?`

- **المتوقع:** المخزون الجاهز من `FG-600` وافر، فيُبلّغ الوكيل بالكفاية **ولا يُنشئ أي عمل**.
- **ما تقوله:** «بلا هذا المشهد يبدو النظام آلةً تولّد مهام. **الامتناع عن العمل نتيجة صحيحة.**»

---

## ٧ب. إرسال صورة إلى الوكيل (جديد)

- **المستخدم:** أي موظف من §٣ في قناته · **الشاشة:** فقاعة المحادثة
- **الإجراء:** اضغط **مشبك الورق** يسار مربع الكتابة ← اختر صورة ← اكتب سؤالك ← أرسل.

```
ما الذي تراه في هذه الصورة؟
```

> *English:* `What do you see in this image?`

- **المتوقع:** يصف الوكيل محتوى الصورة في ردّه ضمن نفس المحادثة وسجل التدقيق المعتاد.
- **ما تقوله للعميل:** «الصورة تمرّ بنفس الحدود: صلاحيات الموظف لم تتغيّر لأنه أرفق صورة.
  إرسال صورة فاتورة لا يمنح وكيل المشتريات صلاحية المحاسبة.»

**الحدود — اذكرها بصراحة إن سُئلت:**

| البند | القيمة |
|---|---|
| الصيغ المقبولة | JPEG · PNG · GIF · WebP **فقط** (ملف PDF ليس صورة ولا يُرسَل) |
| العدد في الرسالة الواحدة | **٤** كحد أقصى |
| الحجم | **٥ ميغابايت** لكل صورة |
| التصغير | الضلع الأطول **1568 بكسل** — ضبط تكلفة، وقد لا تنجو الكتابة الدقيقة جداً |
| الذاكرة | **الصورة لا تُعاد** في الأدوار التالية؛ سؤال متابعة عنها يحتاج إرفاقها مجدداً |
| الصوت | **غير مدعوم** ولم يُطلب |

> ⚠️ ملف غير مقبول **لا يُسقِط السؤال**: يُرفض الملف بجملة واضحة ويُجاب على النص المكتوب.

---

## ٨. مشهد الرفض — اعرض هذا، لا غيره

> ⚠️⚠️⚠️ **هناك نوعان مختلفان من الرفض، والفرق بينهما هو جوهر ما تبيعه.**
>
> **١. رفض النموذج (اعتذار مهذّب).** إذا لم يملك الوكيل **أي أداة** تلمس النموذج المطلوب،
> لا تُستدعى أداة أصلاً ولا يصل الطلب إلى الحارس. النموذج يعتذر بعبارته. **لا يتسرّب شيء**،
> لكن ما شاهده العميل هو **أدب النموذج، لا رفض النواة**.
>
> **٢. رفض النواة (الحدّ الحقيقي).** يظهر فقط عندما **تُستدعى أداة فعلاً** ثم يمنعها الحارس.
> عندها — وعندها فقط — يظهر النص المجمَّد:
> ```
> Refused: this request is outside the agent's authorised scope.
> ```
> ويُكتب السبب الحقيقي (`USER_ACL_DENIED` وغيره) في **سجل التدقيق فقط**.

**هذا هو المشهد الذي تعرضه.** سجّل الدخول بحساب **`fahad.p`** — مشتريات **بصلاحية قراءة فقط** —
وافتح Procurement Intelligence، ثم اكتب **حرفياً**:

```
حضّر مسودة أمر شراء: 4000 من PK-BTL-600 لدى Riyadh PET Co.
```

> *English:* `Prepare a draft purchase order for 4000 units of PK-BTL-600 from supplier Riyadh PET Co.`

> ⚠️⚠️⚠️ **المورّد هنا `Riyadh PET Co.` وليس `Jeddah Plastic Industries` — والفرق يقرّر
> نجاح المشهد.** أداة `prepare_draft_rfq` محميّة من التكرار: إن وُجدت مسودة بنفس
> (الوكيل، الشركة، الصنف، **المورّد**، التاريخ) تُعيدها الأداة **قبل** أن تصل إلى الكتابة —
> فلا كتابة، ولا فحص صلاحية كتابة، **ولا رفض**. وبما أن الخطوة ٣ قد حضّرت مسودة Jeddah
> تلقائياً، فإن سؤال فهد عن Jeddah بعدها **ينجح** ويعيد له رقم المسودة القائمة.
>
> **مقيس يوم 2026-09-09:** فهد + Jeddah ← **ALLOWED**، أُعيد له `P000NN`.
> فهد + Riyadh ← **`USER_ACL_DENIED`**.
>
> النتيجة ليست ثغرة تسريب — فهد يقرأ أوامر الشراء أصلاً — لكنها تعني أن **ترتيب العرض
> يستطيع إسكات مشهد الرفض**. استخدم المورّد الآخر، فيعمل المشهد قبل الخطوة ٣ وبعدها.

- **المتوقع:** النص المجمَّد للمستخدم + **`USER_ACL_DENIED`** في سجل التدقيق.
- **ما تقوله:** «**نفس الوكيل، ونفس الأداة** التي حضّرت المسودة قبل دقائق — والنتيجة رفض،
  **لأن المستخدم مختلف**. هذا هو `USER ∩ AGENT`. لا يمكن للوكيل أن يمنح صلاحية لا يملكها
  الموظف.»

> ⚠️⚠️⚠️ **التحديد شرط.** الصياغة العامة «أنشئ طلب شراء لتغطية نقص العبوات» **لا تُنتج رفض
> النواة**: يسأل الوكيل عن أي منتج تقصد، فلا تُستدعى أداة ولا يُبلَغ الحارس. **اذكر المنتج
> والمورّد والكمية بدقة.**

> ⚠️⚠️ **في سجل التدقيق سترى الاستدعاء المرفوض مرّتين: `ALLOWED` ثم `USER_ACL_DENIED`.**
> هذا **استدعاء واحد مرفوض**، لا استدعاءان: الإذن على مستوى الوكيل مرّ، ثم فحص السجل على
> مستوى **المستخدم** رفض. **لا تعرض صف `ALLOWED` وحده أبداً.**

> ⛔ **لا تُوسَّع أي صلاحية لجعل هذا السؤال ينجح.** نجاحه يعني فشل العرض.

---

## ٩. إعادة التهيئة — قبل العرض وبعده

**الأمر الوحيد المطلوب**، بحساب إداري، **قبل العرض بساعة** (لا بدقيقة):

```python
summary = env['ai.operations.demo.reset'].reset()
env.cr.commit()          # ⚠️ إلزامي — اقرأ التحذير أدناه
print(summary)
```

> ⚠️⚠️⚠️ **بدون `env.cr.commit()` لا يحدث شيء إطلاقاً.** في أودو ١٩ يستدعي
> `odoo/cli/shell.py` الأمر `cr.rollback()` **بعد** تنفيذ السكربت، سواء كانت الجلسة تفاعلية أو
> مُمرَّرة عبر الأنبوب. النتيجة أن إعادة التهيئة **تطبع ملخّصاً صحيحاً وتُلغي كل ما فعلته عند
> الخروج**. تم التحقق من هذا مباشرةً على staging يوم 2026-09-09: سجل أُنشئ داخل الجلسة ثم اختفى،
> وأُعيدت التجربة مع `commit` فبقي. **النسخة السابقة من هذا الدليل قالت إن الجلسة تحفظ عند
> الخروج السليم — وهذا غير صحيح.**

على Odoo.sh — لاحظ `-c ~/.config/odoo/odoo.conf`، فبدونه لا يجد المزوّد مفتاحه:

```bash
ssh <host> 'cd ~ && echo "
summary = env[\"ai.operations.demo.reset\"].reset()
env.cr.commit()
print(summary)
" | PYTHONUNBUFFERED=1 python3 ~/src/odoo/odoo-bin shell \
    -c ~/.config/odoo/odoo.conf -d $DB \
    --addons-path=$(ls -d ~/src/odoo/addons ~/src/enterprise ~/src/user | paste -sd,) \
    --data-dir=/home/odoo/data --no-http --log-level=warn'
```

**الملخّص الراجع يجب أن يحمل `steps_failed` فارغة.** إن لم تكن فارغة، لا تبدأ العرض.

**شكل الملخّص بعد جولة حقيقية** — مقيس على staging يوم 2026-09-09 بعد دور محادثة واحد
وأمر شراء وطلب تسليم:

```
handoffs_cancelled = 1
messages_deleted = 2
purchase_orders_deleted = 1
token_budget_cleared = 1
steps_failed = []
relevelled = {}
```

> ⚠️ **`relevelled = {}` صحيح ما لم تكن قد استلمت بضاعة فعلياً** (الخطوات ٧أ–٧ج). إعادة ضبط
> الكميات تظهر هنا فقط حين يكون هناك ما يُعاد ضبطه، وعندها تتوقّع `{'PK-BTL-600': -4000}`.

> ⚠️ **ملخّص بأصفار في كل الحقول ليس فشلاً** — يعني أنه لا توجد مخلّفات أصلاً (قاعدة نظيفة).
> **تحقّق من خط الأساس بعده**، لا من الملخّص وحده:
> `PK-BTL-600` مطلوب 12,000 / محجوز 8,000 / الفجوة 4,000، وبقية المكوّنات فجوتها صفر.

> 📌 **صفوف التسليم الملغاة (`CANCELLED`) تبقى ظاهرة بعد إعادة التهيئة، وهذا مقصود.** سجل
> التسليم دليل، ولا يملك أي مجموعة صلاحية حذفه. لا تظنّها بقايا عطل.

**خط الأساس بعد إعادة التهيئة — تحقّق منه:**
`PK-BTL-600` مطلوب **12,000** / محجوز **8,000** / الفجوة **4,000**، و`SHORTAGE_COUNT = 1`.

### ماذا يحذف

يعتمد الحذف على **علامات صريحة يكتبها المنتج نفسه**، لا على التاريخ أو المنتج أو المورّد:
أوامر الشراء بمفتاح `ai_idempotency_key`، الأنشطة بـ `ai_profile_code`، سجلات التسليم
(**تُلغى ولا تُحذف** — سجل التسليم دليل، ولا يملك أي مجموعة صلاحية حذفه)، تنبيهات الجودة التي
تبدأ بـ `AI: proposed hold on`، ورسائل قنوات العرض. **ويصفّر عدّاد الاستهلاك اليومي.**

**ويُعيد ضبط المخزون في الاتجاهين** — لأن العرض الحقيقي **يستلم بضاعة فعلية**: يُلغى حجز أمر
التصنيع، تُعاد المكوّنات إلى أرقامها المستهدفة (`PK-BTL-600` إلى 8,000)، ثم يُعاد الحجز.
الفائض يُؤخذ أولاً من الكميات **التي تحمل رقم دفعة** — أي ما استلمه العرض.

### ما لا يمسّه

طلبا البيع `S00662` و`S00663`، أمر التصنيع، بيانات الوثيقة أ كاملة، والقنوات نفسها
(يُفرَّغ محتواها فقط). **هذه حالة البداية، لا مخلّفات.**

> ⚠️⚠️⚠️ **إفراغ القنوات يعني أن الوكيل فقد ذاكرته.** بعد كل إعادة تهيئة **ابدأ من الخطوة ٢**.
> الخطوة ٣ لا تسمّي أمر تصنيع؛ الوكيل يعرف `RM/MO/00002` من سياق الخطوة ٢ وحدها. تشغيلها
> منفردة بعد إعادة تهيئة يجعله يختار من **مئة أمر مفتوح** يقع أمر السيناريو في المرتبة
> العشرين منها — مقيس يوم 2026-09-09: **10,782.72 ريال على `WIP/MO/00223` بدل 312.00 على
> `RM/MO/00002`**. التفصيل الكامل في الخطوة ٣.

> ✅ **الأمر قابل لإعادة التنفيذ بأمان.** مرّتان كمرّة واحدة.
> ⚠️ **لا تُعِد بناء الوحدة كوسيلة لإعادة التهيئة.** إعادة البناء تعيد بناء السيناريو ولا تحذف
> ما أنشأه الوكلاء.

### بعد العرض

نفّذ الأمر نفسه. إن كنت أتممت الخطوة ٩ (الإنتاج) أو ١١ (التسليم)، **تحقّق من خط الأساس
يدوياً** بعدها — إعادة التهيئة تُعيد ضبط المكوّنات لكن أمر تصنيع «تم» لا يعود `confirmed`.
في هذه الحالة أعد بناء السيناريو أو استخدم قاعدة بيانات جديدة قبل العرض التالي.

---

## ١٠. إذا اختلف رقم سجل عمّا هو مكتوب

**هذا متوقَّع.** كل إعادة تهيئة أو جولة تُولّد أرقاماً جديدة لأوامر الشراء وسجلات التسليم.
الأرقام **الثابتة** هي فقط: `S00662` · `S00663` · `RM/MO/00002` · `RM/OUT/00002` · `PK-BTL-600`.

| ما تحتاجه | من أين تقرأه |
|---|---|
| رقم سجل التسليم | AI Operations ← Handoffs ← أحدث صف بحالة `REQUESTED` |
| رقم أمر الشراء المسودة | المشتريات ← طلبات عروض الأسعار ← أحدث مسودة لـ Jeddah Plastic |
| رقم إذن الاستلام | افتح أمر الشراء ← الزر الذكي **الاستلام** |
| رقم إذن التخزين | المخزون ← التحويلات ← نوع Storage في مستودع RM |
| رقم أمر التصنيع | افتح `S00662` ← الزر الذكي **التصنيع** |

**القاعدة:** لا تُملِ رقماً على الوكيل إن لم تكن قد قرأته للتوّ من الشاشة. وإذا كان السؤال
يخصّ أمر التصنيع، **اذكر اسمه `RM/MO/00002`** ودع الأداة تحلّه.

---

## ١١. استكشاف الأخطاء

| العَرَض | السبب | العلاج |
|---|---|---|
| الشاشات فارغة، لا أوامر تصنيع | الشركة النشطة «My Company» | بدّل إلى Naqaa Water Manufacturing Co. |
| لا تظهر فقاعة المحادثة | المستخدم لا يملك `group_ai_user` | اعرض بحساب موظف من §٣ |
| رفض على سؤال عادي كان يعمل صباحاً | **عدّاد الاستهلاك اليومي** | نفّذ §٩ |
| رفض على سؤال عادي، والعدّاد سليم | صلاحية ناقصة أو نموذج خارج القائمة | اقرأ `input_args` و`denial_detail` في سجل التدقيق **قبل** أي تعديل صلاحيات |
| `USER_ACL_DENIED` على قراءة | غالباً «لا يوجد سجل بهذا المعرّف» لا «ليس لك» | أودو لا يكشف وجود السجل؛ افحص المعرّف الذي مرّره النموذج |
| الوكيل يعمل أكثر مما طُلب | صياغة عامة | أضف «فقط. لا تفعل كذا ولا كذا.» |
| الجداول تظهر كرموز `\|` | معروف في Discuss والنافذة المصغّرة | اذكره كملاحظة |
| ردّ فارغ أو انقطاع | مفتاح المزوّد | **لا تطبع المفتاح أبداً** |

---

## ١٢. سجل تنفيذ العرض

| الجولة | البناء | النتيجة |
|---|---|---|
| #12 · #13 | `30036da` | ✅ نظيفتان بالكامل — ١٠/١٠، صفر رفض |
| #16 · #17 | `22d0a2a` | ✅ **نظيفتان بالكامل** — ١٠/١٠، صفر رفض، أمر الشراء **312.00 SAR**، سلسلة الاستلام مغلقة بفجوة صفر |
| تحقّق 2026-09-09 | `21c653f` | ✅ بعد جولة ملاحظات جورج: كل موظف يرى وكيله فقط ويشغّله وحده، التسليم بين التصنيع والمشتريات يعمل، أمر الشراء **312.00 SAR** بـ `approval_required=False`، رفض فهد `USER_ACL_DENIED`، المحاسبة 367,145.85 / 247,735.07، **دور محادثة نصّي كامل عبر المزوّد الحقيقي**، **وصورة حقيقية وصفها النموذج بدقة**، ثم إعادة تهيئة نظيفة أعادت خط الأساس تماماً |

| تحقّق مسار التسليم 2026-09-09 | `6a5e783` | ✅ الخطوة ٢ ثم ٣ في القناة نفسها: طلب تسليم `MATERIAL_SHORTAGE` على `RM/MO/00002` بنقص **4,000**، تشغيل تلقائي بهوية `AI / Procurement`، مسودة أمر شراء **312.00 SAR**، **نشاطان** مُسنَدان إلى نورة، `COMPLETED`، والقفزة الثانية مرفوضة بـ `HANDOFF_CASCADE_BLOCKED` |
| ⚠️ الخطوة ٣ منفردة 2026-09-09 | `6a5e783` | ❌ **لا تفعل هذا.** بلا سياق الخطوة ٢ اختار الوكيل `WIP/MO/00223` فأنتج **138,240 وحدة / 10,782.72 SAR**. الحارس والأداة عملا بشكل سليم (الأداة صحّحت 230,400 — وهي *مطلوب* الأمر لا فجوته — إلى 138,240، فجوته الحقيقية). الخطأ في ترتيب العرض لا في النظام، والمسودة الناتجة تصل **بلا علامة اعتماد** |

**أربع جولات نظيفة متتالية على مزوّد حقيقي.** مقارنة الموردين أعادت 0.078/18 مقابل 0.0827/21
في كل جولة دون استثناء.
